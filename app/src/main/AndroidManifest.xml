<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testcentral">

    <application
        android:name="com.example.testcentral.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestCentral">
        <activity android:name="com.example.testcentral.ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="CHANNEL_CODE"
            android:value="${CHANNEL_CODE}" />

        <provider
            android:name="com.example.testcentral.utils.StartupProvider"
            android:authorities="${applicationId}.android_startup"
            android:exported="false">

            <meta-data
                android:name="com.example.startup.tasks.Task5"
                android:value="android.startup" />
        </provider>
    </application>

</manifest>